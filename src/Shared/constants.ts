export enum Language {
	en = 'en',
	ru = 'ru',
}

export enum Theme {
	light = 'light',
	dark = 'dark',
}
export enum SettingsParams {
	animation = 'animation',
	language = 'language',
	theme = 'theme',
}
