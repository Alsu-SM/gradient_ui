import { Language, Theme } from '../../Shared/constants';

export const DEFAULT_ANIMATION_STATE: boolean = false;
export const DEFAULT_LANGUAGE: Language = Language.en;
export const DEFAULT_THEME: Theme = Theme.light;
